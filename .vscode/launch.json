{
    "version": "0.2.0",
    "configurations": [{
        "name": "Python",
        "type": "python",
        "request": "launch",
        "python": "${command:python.interpreterPath}",
        "program": "${file}",
        "env": {
            "PYTHONPATH": "${workspaceFolder}/src:${env:PYTHONPATH}"
        },
        "windows": {
            "env": {
                "PYTHONPATH": "${workspaceFolder}/src;${env:PYTHONPATH}"
            }
        }
    }]
}
